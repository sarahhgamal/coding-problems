class Solution {
public:
    int removeDuplicates(vector<int>& nums) {
        int left=0;
        int k=0;

        for(int right=1; right<nums.size(); right++)
        {
            if(nums[right]!=nums[left])
            {
                //remove nums[right] and iterate
                //right 
                //idk how to delete the element tho? 
                //do i add both to a new array
                left++;
                nums[left]=nums[right];
            
            } 
        }
        return left + 1 ;
        
    }
};
